# 个人生活主页 API 接口说明文档

## 📋 概述

本文档详细记录了个人生活主页前端调用的所有后端API接口，包括接口地址、参数说明、响应格式等信息。

## 🌐 API 服务器配置

### 服务器地址
前端会根据访问环境自动选择合适的API地址：

| 环境 | 条件 | API地址 | 说明 |
|------|------|---------|------|
| 生产环境(HTTPS) | `life.chenggao.top` + HTTPS | `https://api.chenggao.top` | 避免Mixed Content问题 |
| 生产环境(HTTP) | `life.chenggao.top` + HTTP | `http://api.chenggao.top` | HTTP环境 |
| 本地开发 | 非域名访问 | `http://47.115.72.85:3001` | 本地开发环境 |

### 通用配置
- **超时时间**: 10秒
- **重试次数**: 最多3次，递增延迟
- **缓存时间**: 5分钟
- **请求头**: `Content-Type: application/json`

## 📚 书籍相关接口 (Books API)

### 1. 获取所有书籍
- **接口**: `GET /api/books`
- **描述**: 获取书籍列表，支持查询参数筛选
- **参数**: 
  ```javascript
  // 查询参数（可选）
  {
    status: 'reading|finished',  // 阅读状态
    limit: 10,                   // 限制数量
    page: 1                      // 页码
  }
  ```
- **响应**: 
  ```javascript
  {
    success: true,
    data: [
      {
        id: 1,
        title: "书名",
        author: "作者",
        status: "reading|finished",
        cover: "封面图片URL",
        // ... 其他字段
      }
    ]
  }
  ```

### 2. 获取正在阅读的书籍
- **接口**: `GET /api/books/current`
- **描述**: 获取当前正在阅读的书籍列表
- **参数**: 无
- **响应**: 同上

### 3. 获取已完成的书籍
- **接口**: `GET /api/books/finished`
- **描述**: 获取已读完的书籍列表
- **参数**: 
  ```javascript
  {
    limit: 10  // 限制数量，默认10
  }
  ```
- **响应**: 同上

### 4. 获取单本书籍详情
- **接口**: `GET /api/books/{id}`
- **描述**: 根据书籍ID获取详细信息
- **参数**: 路径参数 `id` (书籍ID)
- **响应**: 
  ```javascript
  {
    success: true,
    data: {
      id: 1,
      title: "书名",
      author: "作者",
      status: "reading|finished",
      cover: "封面图片URL",
      description: "书籍描述",
      thoughts: "个人感悟",
      rating: 5,
      // ... 详细字段
    }
  }
  ```

### 5. 搜索书籍
- **接口**: `GET /api/books/search/{keyword}`
- **描述**: 根据关键词搜索书籍
- **参数**: 
  - 路径参数: `keyword` (搜索关键词，需URL编码)
  - 查询参数: `limit` (限制数量，默认20)
- **响应**: 同书籍列表格式

### 6. 获取书籍统计
- **接口**: `GET /api/books/stats/summary`
- **描述**: 获取书籍相关统计数据
- **参数**: 无
- **响应**: 
  ```javascript
  {
    success: true,
    data: {
      totalBooks: 50,
      readingBooks: 3,
      finishedBooks: 47,
      monthlyReading: 2
    }
  }
  ```

## 🎬 电影相关接口 (Movies API)

### 1. 获取所有电影
- **接口**: `GET /api/movies`
- **描述**: 获取电影列表，支持查询参数筛选
- **参数**: 同书籍接口
- **响应**: 
  ```javascript
  {
    success: true,
    data: [
      {
        id: 1,
        title: "电影名",
        director: "导演",
        year: 2023,
        poster: "海报URL",
        rating: 8.5,
        // ... 其他字段
      }
    ]
  }
  ```

### 2. 获取电影统计
- **接口**: `GET /api/movies/stats`
- **描述**: 获取电影相关统计数据
- **参数**: 无
- **响应**: 统计数据格式

### 3. 获取电影详情
- **接口**: `GET /api/movies/{id}`
- **描述**: 根据电影ID获取详细信息
- **参数**: 路径参数 `id` (电影ID)
- **响应**: 电影详细信息

## 🎵 音乐相关接口 (Music API)

### 1. 获取所有音乐
- **接口**: `GET /api/music`
- **描述**: 获取音乐列表，支持查询参数筛选
- **参数**: 同书籍接口
- **响应**: 
  ```javascript
  {
    success: true,
    data: [
      {
        id: 1,
        title: "歌曲名",
        artist: "艺术家",
        album: "专辑名",
        cover: "专辑封面URL",
        // ... 其他字段
      }
    ]
  }
  ```

### 2. 获取当前播放音乐
- **接口**: `GET /api/music/current`
- **描述**: 获取当前播放或最近播放的音乐
- **参数**: 无
- **响应**: 同音乐列表格式

### 3. 获取音乐详情
- **接口**: `GET /api/music/{id}`
- **描述**: 根据音乐ID获取详细信息
- **参数**: 路径参数 `id` (音乐ID)
- **响应**: 音乐详细信息

## 🏃 马拉松相关接口 (Marathon API)

### 1. 获取所有马拉松记录
- **接口**: `GET /api/marathons`
- **描述**: 获取马拉松比赛记录列表
- **参数**: 同书籍接口
- **响应**: 
  ```javascript
  {
    success: true,
    data: [
      {
        id: 1,
        name: "比赛名称",
        race_date: "2023-10-15",
        location: "比赛地点",
        distance: "42.195",
        finish_time: "04:30:00",
        ranking: 1500,
        total_participants: 5000,
        weather: "晴朗",
        experience: "比赛感悟",
        certificate_image: "证书图片URL",
        medal_image: "奖牌图片URL",
        photos: ["照片URL1", "照片URL2"]
      }
    ]
  }
  ```

### 2. 获取马拉松统计
- **接口**: `GET /api/marathons/stats`
- **描述**: 获取马拉松相关统计数据
- **参数**: 无
- **响应**: 
  ```javascript
  {
    success: true,
    data: {
      total_races: 4,
      total_distance: 168.78,
      average_time: "04:25:00",
      best_time: "04:15:30"
    }
  }
  ```

### 3. 获取马拉松详情
- **接口**: `GET /api/marathons/{id}`
- **描述**: 根据马拉松ID获取详细信息
- **参数**: 路径参数 `id` (马拉松ID)
- **响应**: 马拉松详细信息

## 🔧 系统相关接口 (System API)

### 1. 健康检查
- **接口**: `GET /health`
- **描述**: 检查服务器运行状态
- **参数**: 无
- **响应**: 
  ```javascript
  {
    status: "ok",
    timestamp: "2023-10-15T10:30:00Z"
  }
  ```

### 2. 获取服务器信息
- **接口**: `GET /`
- **描述**: 获取服务器基本信息
- **参数**: 无
- **响应**: 服务器信息

## 📝 问卷调查接口 (Survey API)

### 1. 提交问卷调查
- **接口**: `POST /api/survey/submit`
- **描述**: 提交用户问卷调查数据
- **请求体**: 
  ```javascript
  {
    rating: "5",                    // 评分
    suggestions: "建议内容",        // 建议
    website_interest: "reading",    // 网站兴趣点
    nickname: "用户昵称",           // 昵称（可选）
    contact: "联系方式",            // 联系方式（可选）
    submitted_at: "2023-10-15T10:30:00Z",  // 提交时间
    user_agent: "浏览器信息",       // 用户代理
    referrer: "来源页面"            // 来源页面
  }
  ```
- **响应**: 
  ```javascript
  {
    success: true,
    message: "提交成功",
    id: 123
  }
  ```

## 💌 第三方服务接口

### 1. 留言表单提交 (Formspree)
- **接口**: `POST https://formspree.io/f/mgvzzlog`
- **描述**: 通过Formspree服务提交留言表单
- **请求体**: FormData格式
  ```javascript
  {
    nickname: "用户昵称",     // 昵称（可选）
    email: "邮箱地址",        // 邮箱（可选）
    message: "留言内容"       // 留言内容（必填）
  }
  ```
- **响应**: Formspree标准响应格式

## 🔄 数据加载策略

### 批量数据加载
前端通过 `LifeAPI.loadAllData()` 方法批量加载所有数据：

```javascript
// 并发请求所有数据
const [booksResponse, moviesResponse, musicResponse, marathonResponse] = await Promise.all([
    LifeAPI.getBooks(),
    LifeAPI.getMovies(), 
    LifeAPI.getCurrentMusic(),
    LifeAPI.getMarathons()
]);
```

### 降级策略
1. **API优先**: 优先尝试从后端API获取数据
2. **JSON回退**: API失败时自动回退到本地JSON文件
3. **错误处理**: 提供完整的错误提示和用户反馈

### 缓存机制
- **客户端缓存**: 5分钟内重复请求使用缓存数据
- **缓存键**: 基于API端点生成
- **缓存清理**: 支持手动清除指定或全部缓存

## 🛠️ 错误处理

### 错误类型
1. **网络错误**: 连接失败、超时等
2. **HTTP错误**: 4xx、5xx状态码
3. **数据错误**: 响应格式不正确

### 错误响应格式
```javascript
{
  success: false,
  error: "错误信息",
  code: "ERROR_CODE"
}
```

### 重试机制
- **重试次数**: 最多3次
- **重试延迟**: 递增延迟 (1秒、2秒、3秒)
- **重试条件**: 网络错误或5xx服务器错误

## 📊 前端调用示例

### JavaScript调用示例
```javascript
// 获取书籍数据
try {
  const response = await LifeAPI.getBooks({ status: 'reading', limit: 10 });
  console.log('书籍数据:', response.data);
} catch (error) {
  console.error('获取书籍失败:', error);
  ErrorHandler.showError('数据加载失败，请稍后重试');
}

// 提交问卷调查
try {
  const surveyData = {
    rating: '5',
    suggestions: '网站很棒！',
    website_interest: 'reading'
  };
  const result = await fetch(`${apiBaseUrl}/api/survey/submit`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(surveyData)
  });
  const response = await result.json();
  console.log('提交成功:', response);
} catch (error) {
  console.error('提交失败:', error);
}
```

## 📝 注意事项

1. **CORS配置**: 后端需要正确配置CORS以支持跨域请求
2. **HTTPS支持**: 生产环境建议使用HTTPS避免安全问题
3. **API版本**: 当前为v1版本，后续可能需要版本控制
4. **数据格式**: 所有日期使用ISO 8601格式
5. **图片URL**: 支持相对路径和绝对路径
6. **错误处理**: 前端已实现完整的错误处理和用户提示

---

> 📅 **文档更新时间**: 2025-08-22  
> 🔄 **版本**: v1.0  
> ✍️ **维护者**: 晨昏故人  
> 📧 **联系方式**: 如有问题请通过网站留言功能反馈